# Current Objective

**State**: We are transitioning `app.js` and `style.css` out of a monolithic pattern into `js/` and `css/` subdirectories.

## Tasks
- [x] Splitting CSS
- [x] Splitting JS
- [x] Ensure `index.html` references new imports correctly.
- [ ] Thoroughly test game loop interactions across modules.

*(This file changes week to week depending on the active PRD / Sprint)*
